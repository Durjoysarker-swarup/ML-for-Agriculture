# Requirements for ML-for-Agriculture
# Pin minimum compatible versions where possible; prefer conservative upper bounds to avoid breaking changes.

# Core scientific stack
numpy>=1.24,<2
pandas>=2.0,<3
scipy>=1.9,<2
scikit-learn>=1.2,<2

# Data processing & image handling
opencv-python>=4.7,<5
scikit-image>=0.20,<0.22
Pillow>=9.5,<10

# Gradient boosting libraries (choose one or use multiple as needed)
xgboost>=1.7,<2
lightgbm>=3.3,<4
catboost>=1.1,<1.2

# Optional deep-learning backends (uncomment one depending on project needs)
# tensorflow>=2.12,<3
# torch>=2.1,<3

# Visualization & notebooks
matplotlib>=3.6,<4
seaborn>=0.12,<1
jupyterlab>=4.0,<5
ipython>=8.0,<9

# Explainability / utilities
shap>=0.41,<1
joblib>=1.2,<2
tqdm>=4.65,<5

# Geospatial / agriculture-specific
rasterio>=1.3,<2
geopandas>=0.13,<0.15

# Packaging & installer
pip>=23.0
setuptools>=67.0
wheel

# Notes:
# - Uncomment a deep-learning backend (tensorflow OR torch) only if your project requires it.
# - To create a reproducible environment, consider exporting a lockfile (pip-tools, poetry, or conda).
# - If this repository uses a constraints file or environment.yml, align versions there as well.
